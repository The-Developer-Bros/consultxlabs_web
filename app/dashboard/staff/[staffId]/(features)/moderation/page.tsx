import { ModerationPage } from "@/components/dashboard/shared/ModerationPage";

/** Content moderation — shared with the admin tree; see the component. */
export default function StaffModerationPage() {
  return <ModerationPage />;
}
