import { SupportThreadsPage } from "@/components/dashboard/shared/SupportThreadsPage";

/** #support-hub — per-appointment conversation inbox — shared with the admin tree. */
export default function StaffThreadsPage() {
  return <SupportThreadsPage />;
}
