import { ReferralsPage } from "@/components/dashboard/shared/ReferralsPage";

/** Referrals — shared with the consultee dashboard; see the component. */
export default function ConsultantReferralsPage() {
  return <ReferralsPage role="CONSULTANT" />;
}
