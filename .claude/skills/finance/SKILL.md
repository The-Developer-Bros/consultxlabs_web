---
name: finance
description: Work on this repo's money subsystem — checkout and price derivation, the single-writer payment-confirmation pipeline, payment legs and the funding-leg sum identity, the double-entry ledger and reconciled caches, refunds and credit notes, GST invoicing and the principal-supplier model, Section 194-O TDS withholding and the Income-tax Act 2025 form renumbering, consumer and organisation tax documents, and how to actually verify a money change (Netlify preview, signed webhook replays, Supabase MCP). Use when the user says "checkout", "payment", "refund", "ledger", "GST", "TDS", "tax invoice", "credit note", "webhook not firing", "payment leg", "wallet", "settlement", or is touching lib/payments/, lib/compliance/, lib/enterprise/reachable-paths.ts, app/api/checkout, app/api/webhooks/razorpay, or prisma/sql/payment-legs-triggers.sql. For the Razorpay vendor integration specifically (orders, webhooks, refunds, disputes, payouts, go-live) see references/razorpay/README.md.
---

# Finance

This is the index for the finance domain: how money truth is written and proven correct, and the regime facts the code encodes. The Razorpay vendor pack lives underneath this domain because it is the one gateway the platform integrates with; its own index is `references/razorpay/README.md`.

| Reference                       | Purpose                                                                                                                                                                                                                                                                                                                                             | Read it when                                                                                                          |
| ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `references/doctrine.md`        | The non-negotiable invariants — one transaction holds the truth, CAS-in-WHERE, the single writer, the leg-sum identity and its carve, double-entry with reconciled caches, two-phase refunds, GST as principal supplier, INR-only settlement, the `PG_POOL_MAX=1` trap, best-effort `after()`, business-coded refusals.                             | Before writing or reviewing any change to checkout, a webhook handler, a refund, a payout, or a reconciliation sweep. |
| `references/verification.md`    | How money is proven here — Netlify deploy preview over a local dev server, Razorpay test mode, signed webhook replays, the `/api/dev/mock-webhook` shortcut, driving the sweeps by their HTTP twins, verifying by Supabase MCP SQL rather than narration.                                                                                           | Before claiming a money change is verified.                                                                           |
| `references/compliance.md`      | The regime facts the code encodes — Section 194-O withholding and its three-limb exemption, the Income-tax Act 2025 form renumbering, the GST principal-supplier model and why TCS does not apply, SAC classification, consumer document numbering, IST period boundaries, GSTIN-first supplier state, the outward register, the open CA questions. | Touching `lib/compliance/`, a tax rate, a filing format, or an invoice/credit-note number.                            |
| `references/razorpay/README.md` | The Razorpay vendor integration — orders and checkout, webhooks, refunds, disputes, RazorpayX payouts, GST invoicing, going live, debugging.                                                                                                                                                                                                        | Working on anything under `lib/payments/` or `app/api/webhooks/razorpay`, or when the user says "razorpay".           |

## Non-negotiables

Money truth is written in exactly one Serializable transaction per event, and every status transition moves through a CAS-in-WHERE guard, never a read-then-write. `Payment.paymentStatus` is written by the confirmation pipeline (`handlePaymentSuccess` and its siblings, routed through `routeCapturedPayment`) and by nothing else — see `references/doctrine.md` for the outage this rule prevents. The funding-leg sum identity excludes `REFERRAL_CREDIT` legs, because `Payment.amount` has always meant the post-credit gateway charge; treating that as a defect once broke every credit-funded checkout in production (#1347). The ledger is a double-entry journal; `walletBalance` and the ledger's O(1) balance snapshot are reconciled caches, never a second source of truth. Every refund goes through one of the two front doors and reserves its amount before any gateway call. GST is billed as principal supplier (ADR 26): 18% on the full price, the platform's own tax invoice and credit note, and no Section 52 TCS. The database pool is capped at one connection per function instance, so nothing inside a `$transaction` may read through the global Prisma client. Full detail and sources for every one of these lives in `references/doctrine.md`.
