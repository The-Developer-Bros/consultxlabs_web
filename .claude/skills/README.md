# Claude Code skills index

This repository's project skills are organised by domain. Claude Code loads a project skill only from `.claude/skills/<name>/SKILL.md`, one level deep; the folder name is the slash command (for example `/booking`), and each `SKILL.md`'s frontmatter `description` is what makes the skill trigger — it lists every topic and "use when" phrase the domain absorbed, so a question that used to land on a narrower skill still finds the right one. A skill folder may also hold a `references/` directory of supporting files that the `SKILL.md` points at and Claude reads on demand; those are not themselves discoverable as slash commands.

| Domain         | Command        | Topics                                                                                                                                                                                                                                                                                                                                                                                                                                            | Use it when                                                                                                                                                                       |
| -------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `booking/`     | `/booking`     | Booking doctrine (CAS transitions, never deleting a row a Payment points at, refund front doors, org scoping); availability and slot generation; concurrency (Redis locks, CAS-in-WHERE, Serializable retries); the money boundary (holds, price derivation, refund quotes, funding rails); verification (jest suites, the dev-server recipe, the chaos runbook).                                                                                 | Working on booking, appointment, slot, trial, reschedule, cancellation, refund, availability, allocation, checkout, or expiry-sweep code.                                         |
| `finance/`     | `/finance`     | Money-subsystem doctrine (single writer, CAS-in-WHERE, the leg-sum identity, double-entry ledger, two-phase refunds, GST as principal supplier, `PG_POOL_MAX=1`); how money is verified (Netlify preview, signed webhook replays, Supabase MCP); the regime facts the code encodes (194-O, the 2025 Act renumbering, GST, SAC codes, invoice numbering); the Razorpay vendor integration (orders, webhooks, refunds, disputes, payouts, go-live). | Working on checkout, a payment, a refund, the ledger, GST, TDS, tax invoices, credit notes, or anything under `lib/payments/`, `lib/compliance/`, or `app/api/webhooks/razorpay`. |
| `enterprise/`  | `/enterprise`  | The B2C/B2B funding seam (funding rails, overage handling, wallet debit/credit, invoice rollup, purchase-order draw-down); organisation payouts and TDS (`createOrgPayoutBatch`, the `ORG_PAYOUT` posting, org `TDSRecord`, RazorpayX idempotency).                                                                                                                                                                                               | Working on how an organisation funds bookings, handles overage, or is paid out.                                                                                                   |
| `stream/`      | `/stream`      | Stream.io chat channels, video calls, tokens, webhooks, recordings, moderation, and the crons around them.                                                                                                                                                                                                                                                                                                                                        | Working on chat, meetings, video calls, or anything under `lib/stream/`, `actions/stream/`, `app/api/stream/`, or `app/meetings/`.                                                |
| `maintenance/` | `/maintenance` | The cron architecture (state-as-outbox, the Netlify ticker, `/api/cleanup/*` twins); Netlify environment-variable sync; Next.js/Netlify rendering and caching; Prisma seed-suite sync; serverless-only failure modes (the react-pdf two-React clash, the pool deadlock, `after()` as best-effort, CodeRabbit mechanics).                                                                                                                          | Working on a scheduled job, Netlify environment variables, route caching, seed data, or debugging a 500 that only happens on the deployed build.                                  |
| `workflow/`    | `/workflow`    | PR-comment triage against current code; model orchestration (advisor/teacher/student tiers, effort levels, session-budget economics).                                                                                                                                                                                                                                                                                                             | Triaging PR review comments, or planning and staffing a fleet of subagents.                                                                                                       |

## Formerly

The domains above absorbed several skills that previously stood alone. Each entry below is a deliberate mention for traceability, not a live reference — the content lives at the new path.

| Formerly                 | Now                                                                                    |
| ------------------------ | -------------------------------------------------------------------------------------- |
| `booking-availability`   | `booking/references/availability.md`                                                   |
| `booking-concurrency`    | `booking/references/concurrency.md`                                                    |
| `booking-doctrine`       | `booking/SKILL.md`                                                                     |
| `booking-money-boundary` | `booking/references/money-boundary.md`                                                 |
| `booking-verification`   | `booking/references/verification.md`                                                   |
| `razorpay`               | `finance/references/razorpay/` (its `SKILL.md` is now `references/razorpay/README.md`) |
| `stream-sdk`             | `stream/SKILL.md`                                                                      |
| `netlify-env-sync`       | `maintenance/references/netlify-env-sync.md`                                           |
| `nextjs-netlify-caching` | `maintenance/references/nextjs-netlify-caching.md`                                     |
| `prisma-seed-sync`       | `maintenance/references/prisma-seed-sync.md`                                           |
| `pr-comment-triage`      | `workflow/references/pr-comment-triage.md`                                             |
| `model-orchestration`    | `workflow/references/model-orchestration.md`                                           |
